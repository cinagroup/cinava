# CINA VirtualAgent 品牌检查清单

## 📋 品牌重命名状态

### 已完成的重命名
- ✅ `Oracle VirtualBox` → `CINA VirtualAgent`
- ✅ `Oracle` → `CINASEEK`
- ✅ 版本配置文件已更新（Version.kmk）
- ✅ 版权声明已更新

### 发现的问题
- ⚠️ 仍有大量 `VirtualBox` 字符串存在（代码、注释、变量名等）
- ⚠️ 这些是技术引用（API 名称、文件名、变量名），不需要全部重命名

---

## 🔍 需要检查的品牌点

### 1. 用户可见的品牌名称

| 位置 | 文件 | 状态 |
|------|------|------|
| 产品名称 | README.md | ✅ |
| 版本信息 | Version.kmk | ✅ |
| 版权声明 | 各种文件 | ✅ |
| 服务显示名 | VBoxAutostart-win.cpp | ⚠️ 需检查 |
| 窗口标题 | UI 代码 | ⚠️ 需检查 |
| 关于对话框 | UI 代码 | ⚠️ 需检查 |

### 2. 配置和构建

| 项目 | 文件 | 状态 |
|------|------|------|
| 版本宏 | Version.kmk | ✅ VRA_VERSION_* |
| 构建配置 | Config.kmk | ⚠️ 需检查 |
| 安装程序 | 待定位 | ⚠️ 需检查 |

### 3. 文档和链接

| 项目 | 状态 |
|------|------|
| 用户文档 | ⚠️ 需检查 |
| 开发者文档 | ⚠️ 需检查 |
| 网站链接 | ⚠️ 需检查 |

---

## 📊 统计信息（初步）

```
总文件数: 64,639
包含 "VirtualBox" 的文件: 大量
包含 "Oracle" 的文件: 待统计
```

---

## 🎯 优先级

### P0 - 必须修改（用户可见）
- [ ] 产品名称在 UI 中的显示
- [ ] 安装程序中的品牌
- [ ] 关于对话框
- [ ] 窗口标题

### P1 - 应该修改（文档和配置）
- [ ] README.md 和其他文档
- [ ] 构建配置中的产品名称
- [ ] 版权声明（已完成）

### P2 - 可以保持（技术引用）
- [ ] API 名称（IVirtualBox, CLSID_VirtualBox 等）
- [ ] 变量名（g_pVirtualBox 等）
- [ ] 文件名和路径
- [ ] 内部代码注释

---

## 🔧 检查命令

```bash
# 查找用户可见的产品名称
grep -r "VirtualBox Autostart Service" ./src

# 查找版本相关
grep -r "VBOX_PRODUCT" ./include ./src

# 查找 UI 相关
find ./src -name "*.ui" -o -name "*.qrc"
```

---

## 📝 备注

- 技术引用（API 名称、变量名）不需要重命名，保持兼容性
- 重点关注用户可见的品牌元素
- 建议先在一个平台上构建测试，再进行大规模修改
